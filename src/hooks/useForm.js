/**
 * Wrapper sobre react-hook-form com integração ao sistema de validação.
 * Re-exporta o hook original já configurado com o idioma PT-BR.
 */
export { useForm, useFieldArray, useWatch, Controller } from 'react-hook-form'
